% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariate.R
\name{covt_roots_lin}
\alias{covt_roots_lin}
\title{Compute curve parameter and distance for each spot (linear curve)}
\usage{
covt_roots_lin(
  spatial,
  xcoef,
  ycoef,
  trimmed,
  start = NULL,
  end = NULL,
  cores = 1L
)
}
\arguments{
\item{spatial}{A data frame of spatial coordinates}

\item{xcoef}{Polynomial coefficients for x(t)}

\item{ycoef}{Polynomial coefficients for y(t)}

\item{trimmed}{Logical. Whether the curve is trimmed}

\item{start}{Starting coordinate of the trimmed curve (required if `trimmed = TRUE`)}

\item{end}{Ending coordinate of the trimmed curve (required if `trimmed = TRUE`)}

\item{cores}{Number of cores for parallel computation (not supported on Windows)}
}
\value{
A data frame with additional columns: `t` (curve parameter) and `distance` (Euclidean distance to curve)
}
\description{
Computes the curve parameter (t) and Euclidean distance from each spatial spot to a linear curve axis (CA).  
Supports both full and trimmed linear curves.
}
