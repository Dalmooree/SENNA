% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{Make_simplesktS}
\alias{Make_simplesktS}
\title{Create a `simplesktS` object for SVG pattern sketching}
\usage{
Make_simplesktS(
  senna,
  genelist,
  nbins = NULL,
  interval = sqrt(2),
  thr = 0.01,
  active = FALSE
)
}
\arguments{
\item{senna}{A `SENNA`, `msR`, or `sktS` object}

\item{genelist}{A character vector of gene names to include}

\item{nbins}{An integer specifying the number of bins for curve segmentation.
If `NULL`, the curve will be segmented around each knot using a fixed radius (`thr`).}

\item{interval}{The inclusion radius used to select spots near the curve axis (default = `sqrt(2)`)}

\item{thr}{Radius threshold used when `nbins = NULL` (default = `0.01`)}

\item{active}{If `TRUE`, restrict to activated spots as defined in `Coord$Activated`}
}
\value{
A `simplesktS` object containing the gene sketch matrix and spot count per segment
}
\description{
Generate a simple sketch matrix representing average expression patterns  
of selected genes along the curve axis, using either knot-centered or  
evenly binned segments depending on `nbins`.
}
